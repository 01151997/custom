import { ApptestdirectiveDirective } from './apptestdirective.directive';

describe('ApptestdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ApptestdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
